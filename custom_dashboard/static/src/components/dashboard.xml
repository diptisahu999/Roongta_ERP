<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="custom_dashboard.DashboardView" owl="1">
        <div class="custom-dashboard-wrapper">
            <!-- 1. Top Greeting & Action Bar -->
            <div class="dash-header">
                <div class="dash-greeting-box">
                    <div class="dash-greeting-top-row">
                        <h1>
                            <t t-if="state.data.user_info and state.data.user_info.greeting">
                                <t t-esc="state.data.user_info.greeting"/>
                            </t>
                            <t t-else="">
                                Good Afternoon, User 👋
                            </t>
                        </h1>
                        <!-- Date Pill: Friday, 04 Sep 2026 -->
                        <div class="dash-date-pill">
                            <i class="fa fa-calendar-o dash-date-icon"></i>
                            <span class="dash-date-text"><t t-esc="currentDateFormatted"/></span>
                        </div>
                    </div>
                    <p class="dash-greeting-subtitle">Real-time enterprise operational intelligence &amp; task command center.</p>
                </div>

                <div class="dash-header-actions">
                    <!-- Refresh Button -->
                    <button class="dash-btn-refresh" t-on-click="() => this.loadDashboardData()" title="Refresh Dashboard" t-att-disabled="state.loading">
                        <i t-attf-class="fa fa-refresh #{state.loading ? 'fa-spin' : ''}"></i>
                    </button>

                    <button class="btn-create-task" t-on-click="() => this.onCreateTask()">
                        <i class="fa fa-plus"></i>
                        <span>Create Task</span>
                    </button>

                    <div class="dash-search-container">
                        <i class="fa fa-search dash-search-icon"></i>
                        <input type="text"
                               t-ref="searchInput"
                               placeholder="Search tasks..."
                               t-on-input="(ev) => this.onSearchInput(ev)"/>
                        <span class="dash-search-shortcut">Ctrl K</span>
                    </div>

                    <div class="dash-user-pill">
                        <img class="dash-user-avatar"
                             t-att-src="state.data.user_info and state.data.user_info.avatar_url ? state.data.user_info.avatar_url : '/web/static/img/user_placeholder.jpg'"
                             alt="User Avatar"/>
                        <div class="dash-user-info-text">
                            <span class="dash-user-name">
                                <t t-esc="state.data.user_info ? state.data.user_info.name : 'Testing Admin'"/>
                            </span>
                            <!-- Hidden as requested: 
                            <span class="dash-user-role">
                                <t t-esc="state.data.user_info ? state.data.user_info.role : 'Administrator'"/>
                            </span>
                            -->
                        </div>
                    </div>
                </div>
            </div>

            <!-- 2. Filter Controls Bar -->
            <div class="dash-filters-bar">
                <!-- Left: Quick Scope Pills -->
                <div class="dash-quick-tabs">
                    <button class="dash-tab-btn" 
                            t-att-class="{'active': state.filters.custom_view === 'all' or !state.filters.custom_view}"
                            t-on-click="() => this.setQuickFilter('all')">
                        All Tasks
                    </button>
                    <button class="dash-tab-btn" 
                            t-att-class="{'active': state.filters.custom_view === 'my_tasks'}"
                            t-on-click="() => this.setQuickFilter('my_tasks')">
                        My Tasks
                    </button>
                    <button class="dash-tab-btn" 
                            t-att-class="{'active': state.filters.custom_view === 'due_this_week'}"
                            t-on-click="() => this.setQuickFilter('due_this_week')">
                        Due This Week
                    </button>
                    <button class="dash-tab-btn" 
                            t-att-class="{'active': state.filters.custom_view === 'high_priority'}"
                            t-on-click="() => this.setQuickFilter('high_priority')">
                        ★ High Priority
                    </button>
                </div>

                <!-- Right: Dropdown Select Filters -->
                <div class="dash-filters-right">
                    <!-- Company Filter -->
                    <div class="dash-select-wrapper">
                        <i class="fa fa-th-large prefix-icon"></i>
                        <select class="dash-filter-select" t-on-change="(ev) => this.onFilterChange('company_id', ev)">
                            <option value="">All Companies</option>
                            <t t-foreach="state.data.companies or []" t-as="comp" t-key="comp.id">
                                <option t-att-value="comp.id" t-att-selected="state.filters.company_id == comp.id">
                                    <t t-esc="comp.name"/>
                                </option>
                            </t>
                        </select>
                        <i class="fa fa-chevron-down suffix-chevron"></i>
                    </div>

                    <!-- Department Filter -->
                    <div class="dash-select-wrapper">
                        <i class="fa fa-users prefix-icon"></i>
                        <select class="dash-filter-select" t-on-change="(ev) => this.onFilterChange('department_id', ev)">
                            <option value="">All Departments</option>
                            <t t-foreach="state.data.departments or []" t-as="dept" t-key="dept.id">
                                <option t-att-value="dept.id" t-att-selected="state.filters.department_id == dept.id">
                                    <t t-esc="dept.name"/>
                                </option>
                            </t>
                        </select>
                        <i class="fa fa-chevron-down suffix-chevron"></i>
                    </div>

                    <!-- Assignee Filter -->
                    <div class="dash-select-wrapper">
                        <i class="fa fa-user-o prefix-icon"></i>
                        <select class="dash-filter-select" t-on-change="(ev) => this.onFilterChange('user_id', ev)">
                            <option value="">All Assignees</option>
                            <t t-foreach="state.data.assignees or []" t-as="user" t-key="user.id">
                                <option t-att-value="user.id" t-att-selected="state.filters.user_id == user.id">
                                    <t t-esc="user.name"/>
                                </option>
                            </t>
                        </select>
                        <i class="fa fa-chevron-down suffix-chevron"></i>
                    </div>

                    <!-- Time Period Filter -->
                    <div class="dash-select-wrapper">
                        <i class="fa fa-calendar-o prefix-icon"></i>
                        <select class="dash-filter-select" t-on-change="(ev) => this.onFilterChange('date_range', ev)">
                            <option value="all" t-att-selected="state.filters.date_range == 'all' or !state.filters.date_range">All Time</option>
                            <option value="today" t-att-selected="state.filters.date_range == 'today'">Today</option>
                            <option value="this_week" t-att-selected="state.filters.date_range == 'this_week'">This Week</option>
                            <option value="this_month" t-att-selected="state.filters.date_range == 'this_month'">This Month</option>
                        </select>
                        <i class="fa fa-chevron-down suffix-chevron"></i>
                    </div>
                </div>
            </div>

            <!-- 3. Top KPI Metric Cards (5 Cards) -->
            <div class="dash-kpis-grid" t-if="state.data.kpis">
                <!-- Card 1: TOTAL TASKS -->
                <div class="dash-kpi-card" t-on-click="() => this.onOpenTaskList('all')">
                    <div style="position: absolute; top: 0; left: 0; right: 0; height: 4px; background-color: #8b5cf6;"/>
                    <div class="dash-kpi-top">
                        <div class="dash-kpi-icon-box purple">
                            <i class="fa fa-clipboard"></i>
                        </div>
                        <span class="dash-kpi-title">TOTAL TASKS</span>
                    </div>
                    <div class="dash-kpi-value">
                        <t t-esc="state.data.kpis.total_tasks.value"/>
                    </div>
                    <div class="dash-kpi-footer trend-up">
                        <i class="fa fa-arrow-up"></i>
                        <t t-esc="state.data.kpis.total_tasks.trend"/>
                    </div>
                </div>

                <!-- Card 2: IN PROGRESS -->
                <div class="dash-kpi-card" t-on-click="() => this.onOpenTaskList('in_progress')">
                    <div style="position: absolute; top: 0; left: 0; right: 0; height: 4px; background-color: #3b82f6;"/>
                    <div class="dash-kpi-top">
                        <div class="dash-kpi-icon-box blue">
                            <i class="fa fa-repeat"></i>
                        </div>
                        <span class="dash-kpi-title">IN PROGRESS</span>
                    </div>
                    <div class="dash-kpi-value">
                        <t t-esc="state.data.kpis.in_progress.value"/>
                    </div>
                    <div class="dash-kpi-footer trend-blue">
                        <t t-esc="state.data.kpis.in_progress.subtext"/>
                    </div>
                </div>

                <!-- Card 3: COMPLETED -->
                <div class="dash-kpi-card" t-on-click="() => this.onOpenTaskList('completed')">
                    <div style="position: absolute; top: 0; left: 0; right: 0; height: 4px; background-color: #10b981;"/>
                    <div class="dash-kpi-top">
                        <div class="dash-kpi-icon-box green">
                            <i class="fa fa-check-circle"></i>
                        </div>
                        <span class="dash-kpi-title">COMPLETED</span>
                    </div>
                    <div class="dash-kpi-value">
                        <t t-esc="state.data.kpis.completed.value"/>
                    </div>
                    <div class="dash-kpi-footer trend-green">
                        <t t-esc="state.data.kpis.completed.subtext"/>
                    </div>
                </div>

                <!-- Card 4: DUE TODAY -->
                <div class="dash-kpi-card" t-on-click="() => this.onOpenTaskList('due_today')">
                    <div style="position: absolute; top: 0; left: 0; right: 0; height: 4px; background-color: #f59e0b;"/>
                    <div class="dash-kpi-top">
                        <div class="dash-kpi-icon-box orange">
                            <i class="fa fa-calendar-check-o"></i>
                        </div>
                        <span class="dash-kpi-title">DUE TODAY</span>
                    </div>
                    <div class="dash-kpi-value">
                        <t t-esc="state.data.kpis.due_today.value"/>
                    </div>
                    <div class="dash-kpi-footer trend-orange">
                        <t t-esc="state.data.kpis.due_today.subtext"/>
                    </div>
                </div>

                <!-- Card 5: OVERDUE -->
                <div class="dash-kpi-card" t-on-click="() => this.onOpenTaskList('overdue')">
                    <div style="position: absolute; top: 0; left: 0; right: 0; height: 4px; background-color: #ef4444;"/>
                    <div class="dash-kpi-top">
                        <div class="dash-kpi-icon-box red">
                            <i class="fa fa-exclamation"></i>
                        </div>
                        <span class="dash-kpi-title">OVERDUE</span>
                    </div>
                    <div class="dash-kpi-value">
                        <t t-esc="state.data.kpis.overdue.value"/>
                    </div>
                    <div class="dash-kpi-footer trend-red">
                        <i class="fa fa-arrow-up"></i>
                        <t t-esc="state.data.kpis.overdue.trend"/>
                    </div>
                </div>
            </div>

            <!-- 4. Middle Section (3 Widgets Grid) -->
            <div class="dash-middle-grid">
                <!-- Widget 1: OVERALL TASK PROGRESS -->
                <div class="dash-widget-card">
                    <div class="dash-widget-header">
                        <span class="dash-widget-title">OVERALL TASK PROGRESS</span>
                    </div>
                    <div class="dash-progress-content">
                        <!-- Circular Gauge SVG -->
                        <div class="dash-gauge-container">
                            <svg class="dash-gauge-svg" viewBox="0 0 120 120">
                                <circle class="dash-gauge-bg" cx="60" cy="60" r="52"/>
                                <circle class="dash-gauge-fill" cx="60" cy="60" r="52"
                                        t-att-stroke-dasharray="gaugeCircumference"
                                        t-att-stroke-dashoffset="gaugeDashOffset"/>
                            </svg>
                            <div class="dash-gauge-text">
                                <span class="dash-gauge-percent">
                                    <t t-esc="state.data.progress_breakdown ? state.data.progress_breakdown.percentage : 72"/>%
                                </span>
                                <span class="dash-gauge-label">Overall Progress</span>
                            </div>
                        </div>

                        <!-- Legend & Breakdown -->
                        <div class="dash-progress-breakdown" t-if="state.data.progress_breakdown">
                            <div class="dash-breakdown-row completed">
                                <span class="dash-breakdown-dot"></span>
                                <span class="dash-breakdown-count"><t t-esc="state.data.progress_breakdown.completed"/></span>
                                <span>Completed</span>
                            </div>
                            <div class="dash-breakdown-row in-progress">
                                <span class="dash-breakdown-dot"></span>
                                <span class="dash-breakdown-count"><t t-esc="state.data.progress_breakdown.in-progress or state.data.progress_breakdown.in_progress"/></span>
                                <span>In Progress</span>
                            </div>
                            <div class="dash-breakdown-row pending">
                                <span class="dash-breakdown-dot"></span>
                                <span class="dash-breakdown-count"><t t-esc="state.data.progress_breakdown.pending"/></span>
                                <span>Pending</span>
                            </div>
                            <div class="dash-breakdown-row blocked">
                                <span class="dash-breakdown-dot"></span>
                                <span class="dash-breakdown-count"><t t-esc="state.data.progress_breakdown.blocked"/></span>
                                <span>Blocked</span>
                            </div>
                            <div class="dash-progress-total">
                                <t t-esc="state.data.progress_breakdown.total"/> Total Tasks
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Widget 2: ATTENTION REQUIRED -->
                <div class="dash-widget-card">
                    <div class="dash-widget-header">
                        <span class="dash-widget-title">ATTENTION REQUIRED</span>
                    </div>
                    <div class="dash-attention-list">
                        <t t-foreach="state.data.attention_required or []" t-as="item" t-key="item.id">
                            <div class="dash-attention-row" t-on-click="() => this.onOpenTaskList(item.id)">
                                <div class="dash-attention-left">
                                    <div class="dash-attention-icon"
                                         t-attf-style="background-color: #{item.bg_color}; color: #{item.color};">
                                        <t t-if="item.id == 'overdue'">!</t>
                                        <t t-elif="item.id == 'due_today'"><i class="fa fa-calendar-check-o"></i></t>
                                        <t t-elif="item.id == 'due_this_week'"><i class="fa fa-clock-o"></i></t>
                                        <t t-elif="item.id == 'blocked'"><i class="fa fa-ban"></i></t>
                                        <t t-else=""><i class="fa fa-user"></i></t>
                                    </div>
                                    <span class="dash-attention-label">
                                        <strong><t t-esc="item.count"/></strong> <t t-esc="item.name"/>
                                    </span>
                                </div>
                                <div class="dash-attention-right">
                                    <i class="fa fa-chevron-right"></i>
                                </div>
                            </div>
                        </t>
                    </div>
                </div>

                <!-- Widget 3: TEAM WORKLOAD -->
                <div class="dash-widget-card">
                    <div class="dash-widget-header">
                        <span class="dash-widget-title">TEAM WORKLOAD</span>
                    </div>
                    <div class="dash-workload-list">
                        <t t-foreach="state.data.team_workload or []" t-as="team" t-key="team.id">
                            <div class="dash-workload-row" t-on-click="() => this.onOpenDepartmentTasks(team.id)" title="Click to view department tasks">
                                <div class="dash-team-avatar-wrapper">
                                    <img class="dash-team-avatar-img"
                                         t-if="team.lead_avatar"
                                         t-att-src="team.lead_avatar"
                                         alt="Team Lead"/>
                                    <div class="dash-team-avatar" t-else="">
                                        <t t-esc="team.avatar_text"/>
                                    </div>
                                </div>
                                <div class="dash-workload-content">
                                    <div class="dash-workload-top">
                                        <div class="dash-team-name-group">
                                            <span class="dash-team-name"><t t-esc="team.name"/></span>
                                            <span class="dash-team-status" t-attf-class="dash-status-{{ team.status_class }}"><t t-esc="team.status"/></span>
                                        </div>
                                        <div class="dash-workload-bar-container">
                                            <div class="dash-workload-bar-bg">
                                                <div class="dash-workload-bar-fill" t-attf-style="width: #{team.percentage}%;"></div>
                                            </div>
                                        </div>
                                        <div class="dash-workload-pct">
                                            <t t-esc="team.percentage"/>%
                                        </div>
                                    </div>
                                    <div class="dash-workload-bottom">
                                        <div class="dash-team-avatars-stack" t-if="team.members and team.members.length">
                                            <t t-foreach="team.members" t-as="m" t-key="m.id">
                                                <div class="dash-stack-avatar" t-att-title="m.name">
                                                    <t t-esc="m.initial"/>
                                                </div>
                                            </t>
                                        </div>
                                        <div class="dash-workload-meta">
                                            <span><t t-esc="team.done_tasks"/> of <t t-esc="team.total_tasks"/> tasks</span>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </t>
                        <div class="text-center py-4 text-muted" t-if="!state.data.team_workload or !state.data.team_workload.length">
                            <i class="fa fa-users mb-2" style="font-size: 24px; opacity: 0.4;"></i>
                            <div style="font-size: 13px;">No department workload data</div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- 5. Overdue Tasks Table Section -->
            <div class="dash-table-card">
                <div class="pd-odoo-control-panel" style="padding: 12px 16px; display: flex; justify-content: space-between; align-items: center; border-bottom: 1px solid #e2e8f0; background: white; border-top-left-radius: 12px; border-top-right-radius: 12px;">
                    <div class="pd-cp-left">
                        <span class="pd-cp-breadcrumb" style="font-weight: bold; font-size: 15px; color: #334155; text-transform: uppercase;">OVERDUE TASKS</span>
                    </div>
                    <div class="pd-cp-right" style="display: flex; gap: 12px; align-items: center; flex-wrap: nowrap; flex-shrink: 0;">
                        <select t-model="state.overdueDeptFilter" class="form-select form-select-sm" style="min-width: 160px; cursor: pointer; font-size: 13px; font-weight: 500; color: #475569; background-color: #f8fafc; border-color: #cbd5e1; box-shadow: 0 1px 2px rgba(0,0,0,0.05);">
                            <option value="">All Departments</option>
                            <t t-foreach="this.getOverdueDepartments()" t-as="dept" t-key="dept">
                                <option t-att-value="dept" t-esc="dept"/>
                            </t>
                        </select>
                        <select t-model="state.overdueProjectFilter" class="form-select form-select-sm" style="min-width: 160px; cursor: pointer; font-size: 13px; font-weight: 500; color: #475569; background-color: #f8fafc; border-color: #cbd5e1; box-shadow: 0 1px 2px rgba(0,0,0,0.05);">
                            <option value="">All Projects</option>
                            <t t-foreach="this.getOverdueProjects()" t-as="proj" t-key="proj">
                                <option t-att-value="proj" t-esc="proj"/>
                            </t>
                        </select>
                        <a class="dash-widget-link" t-on-click="() => this.onOpenTaskList('overdue')" style="margin-left: 8px; white-space: nowrap; flex-shrink: 0;">View All</a>
                    </div>
                </div>
                <div class="pd-odoo-list-box" style="margin-top: 16px;">
                    <div class="pd-table-scroll-wrapper pd-odoo-table-scroll" style="max-height: 400px; overflow-y: auto;">
                        <table class="pd-grouped-table pd-odoo-tree-table" style="width: 100%; border-collapse: collapse; text-align: left;">
                            <thead>
                                <tr class="pd-th-row">
                                    <th class="pd-th-cb" style="width: 32px; text-align: center;"><input type="checkbox" class="pd-odoo-cb" disabled="disabled"/></th>
                                    <th class="pd-th-star" style="width: 26px; text-align: center;"><i class="fa fa-star-o"/></th>
                                    <th class="pd-th-title" style="min-width: 180px;">Title</th>
                                    <th class="pd-th-project" style="min-width: 105px;">Project</th>
                                    <th class="pd-th-creator" style="min-width: 125px;">Created By</th>
                                    <th class="pd-th-assignees" style="min-width: 85px;">Assignees</th>
                                    <th class="pd-th-progbar" style="min-width: 75px;">Progress Bar</th>
                                    <th class="pd-th-days" style="min-width: 65px; text-align: center;">Days Open</th>
                                    <th class="pd-th-deadline" style="min-width: 105px;">Date Deadline</th>
                                    <th class="pd-th-activity" style="min-width: 70px; text-align: center;">Next Activity</th>
                                    <th class="pd-th-tags" style="min-width: 75px;">Tags</th>
                                    <th class="pd-th-stage" style="min-width: 95px;">Stage</th>
                                </tr>
                            </thead>
                            <tbody>
                                <t t-if="!filteredTableGroups or !filteredTableGroups.length">
                                    <tr>
                                        <td colspan="12" style="text-align: center; padding: 28px; color: #94a3b8; font-size: 13px;">
                                            No overdue tasks currently available.
                                        </td>
                                    </tr>
                                </t>
                                <t t-else="">
                                    <t t-foreach="filteredTableGroups" t-as="group" t-key="group.name">
                                        <!-- Group Accordion Header -->
                                        <tr class="pd-grp-hdr-row pd-odoo-grp-hdr-row" t-on-click="() => this.toggleGroup(group.name)">
                                            <td colspan="12">
                                                <span class="pd-grp-chevron">
                                                    <t t-if="state.collapsedGroups[group.name]">▶</t>
                                                    <t t-else="">▼</t>
                                                </span>
                                                <b t-esc="(group.name || 'Group').toUpperCase()"/> (<t t-esc="group.count || 0"/>)
                                            </td>
                                        </tr>
                                        
                                        <!-- Group Task Rows -->
                                        <t t-if="!state.collapsedGroups[group.name]">
                                            <t t-foreach="group.tasks" t-as="task" t-key="task.id">
                                                <tr class="pd-task-item-row pd-odoo-task-row" t-on-click="() => this.onOpenTask(task.id)" title="Click to view task details">
                                                    <td class="pd-td-cb" style="text-align: center;" t-on-click.stop="">
                                                        <input type="checkbox" class="pd-odoo-cb"/>
                                                    </td>
                                                    <td class="pd-td-star" style="text-align: center;" t-on-click.stop="(ev) => this.toggleStar(task, ev)">
                                                        <span class="pd-task-star" t-att-style="isStarred(task) ? 'color: #f59e0b;' : 'color: #cbd5e1;'">
                                                            <t t-if="isStarred(task)">★</t>
                                                            <t t-else="">☆</t>
                                                        </span>
                                                    </td>
                                                    <td class="pd-task-title-cell">
                                                        <span class="pd-odoo-task-title" t-att-title="task.title || ''" t-esc="task.title || ''"/>
                                                    </td>
                                                    <td class="pd-truncate" t-att-title="task.project || ''" t-esc="task.project || ''"/>
                                                    <td>
                                                        <div class="pd-avatar-badge-wrap" t-att-title="task.created_by">
                                                            <span class="pd-avatar-badge" style="background-color: #875A7B; color: #ffffff;">
                                                                <t t-esc="task.created_by_initial || 'U'"/>
                                                            </span>
                                                            <span class="pd-avatar-name" t-esc="task.created_by || ''"/>
                                                        </div>
                                                    </td>
                                                    <td>
                                                        <div class="pd-assignees-stack">
                                                            <t t-if="task.assignees &amp;&amp; task.assignees.length">
                                                                <t t-foreach="task.assignees" t-as="a" t-key="a.name">
                                                                    <span class="pd-avatar-badge" t-att-title="a.name || ''" style="background-color: #875A7B; color: #ffffff;">
                                                                        <t t-esc="a.initial || 'U'"/>
                                                                    </span>
                                                                </t>
                                                            </t>
                                                        </div>
                                                    </td>
                                                    <td>
                                                        <div class="pd-odoo-prog-box-wrap">
                                                            <span class="pd-odoo-prog-box" t-att-style="task.progress &gt;= 100 ? 'background-color: #00878a; border-color: #00878a;' : (task.progress &gt; 0 ? 'background: linear-gradient(to right, #00878a ' + task.progress + '%, #ffffff ' + task.progress + '%);' : '')"></span>
                                                            <span class="pd-odoo-prog-text"><t t-esc="task.progress || 0"/> %</span>
                                                        </div>
                                                    </td>
                                                    <td style="text-align: center; color: #ef4444; font-weight: 600;" t-esc="task.days_open || 0"/>
                                                    <td>
                                                        <span class="pd-deadline-overdue" t-esc="task.date_deadline || ''" style="color: #ef4444;"/>
                                                    </td>
                                                    <td style="text-align: center;">
                                                        <i class="fa fa-clock-o pd-act-clock-icon" title="Next Activity"/>
                                                    </td>
                                                    <td>
                                                        <span class="pd-odoo-tag-pill" t-if="task.tag" t-esc="task.tag"/>
                                                    </td>
                                                    <td>
                                                        <span class="pd-odoo-stage-text" t-att-title="task.stage || ''" t-esc="task.stage || ''"/>
                                                    </td>
                                                </tr>
                                            </t>
                                        </t>
                                    </t>
                                </t>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>

            <!-- 6. Bottom Section (2 Widgets Grid) -->
            <div class="dash-bottom-grid">
                <!-- Widget Left: TASK COMPLETION TREND -->
                <div class="dash-widget-card">
                    <div class="dash-widget-header">
                        <span class="dash-widget-title">TASK COMPLETION TREND</span>
                        <div class="dash-select-wrapper">
                            <select class="dash-filter-select-mini" t-on-change="(ev) => this.onTrendPeriodChange(ev)">
                                <option value="7_days" t-att-selected="state.trendPeriod == '7_days'">7 Days</option>
                                <option value="30_days" t-att-selected="state.trendPeriod == '30_days'">30 Days</option>
                            </select>
                            <i class="fa fa-chevron-down suffix-chevron" style="right: 8px; font-size: 10px;"></i>
                        </div>
                    </div>

                    <!-- SVG Curve Area Chart -->
                    <div class="dash-trend-container">
                        <!-- Active Hover Tooltip -->
                        <div class="dash-trend-tooltip"
                             t-if="activeTooltipPos"
                             t-attf-style="left: #{activeTooltipPos.xPct}%; top: #{activeTooltipPos.yPct}%;">
                            <div class="dash-tooltip-date"><t t-esc="activeTooltipPos.date"/></div>
                            <div class="dash-tooltip-tasks"><t t-esc="activeTooltipPos.count"/> Tasks</div>
                        </div>

                        <svg class="dash-trend-svg" viewBox="0 0 540 210">
                            <defs>
                                <linearGradient id="trendPurpleGradient" x1="0%" y1="0%" x2="0%" y2="100%">
                                    <stop offset="0%" stop-color="#6366f1" stop-opacity="0.30"/>
                                    <stop offset="100%" stop-color="#6366f1" stop-opacity="0.0"/>
                                </linearGradient>
                            </defs>

                            <!-- Horizontal Grid Lines -->
                            <line class="dash-trend-grid-line" x1="40" y1="25" x2="520" y2="25"/>
                            <text class="dash-trend-axis-text" x="14" y="29">80</text>

                            <line class="dash-trend-grid-line" x1="40" y1="62.5" x2="520" y2="62.5"/>
                            <text class="dash-trend-axis-text" x="14" y="66.5">60</text>

                            <line class="dash-trend-grid-line" x1="40" y1="100" x2="520" y2="100"/>
                            <text class="dash-trend-axis-text" x="14" y="104">40</text>

                            <line class="dash-trend-grid-line" x1="40" y1="137.5" x2="520" y2="137.5"/>
                            <text class="dash-trend-axis-text" x="14" y="141.5">20</text>

                            <line class="dash-trend-grid-line" x1="40" y1="175" x2="520" y2="175"/>
                            <text class="dash-trend-axis-text" x="20" y="179">0</text>

                            <!-- Area Gradient Path -->
                            <path class="dash-trend-area" t-att-d="trendPointsData.areaPath"/>

                            <!-- Smooth Curve Line -->
                            <path class="dash-trend-line" t-att-d="trendPointsData.linePath"/>

                            <!-- Data Dots & X-axis Labels -->
                            <t t-foreach="trendPointsData.coords" t-as="pt" t-key="pt.raw.date || pt.raw.day">
                                <circle t-att-cx="pt.x" t-att-cy="pt.y" r="4"
                                        t-attf-class="dash-trend-point #{pt.raw.is_peak ? 'peak' : ''}"
                                        t-on-mouseenter="() => this.onHoverTrendPoint(pt)"/>
                                <text class="dash-trend-axis-text"
                                      t-att-x="pt.x" y="198" text-anchor="middle">
                                    <t t-esc="pt.raw.day"/>
                                </text>
                            </t>
                        </svg>
                    </div>
                </div>

                <!-- Widget Right: RECENT ACTIVITY -->
                <div class="dash-widget-card">
                    <div class="dash-widget-header">
                        <span class="dash-widget-title">RECENT ACTIVITY</span>
                    </div>
                    <div class="dash-activity-list">
                        <t t-foreach="state.data.recent_activity or []" t-as="act" t-key="act.id">
                            <div class="dash-activity-item">
                                <div class="dash-activity-icon"
                                     t-attf-style="background-color: #{act.color}18; color: #{act.color};">
                                    <t t-if="act.icon == 'check-circle'"><i class="fa fa-check-circle"></i></t>
                                    <t t-elif="act.icon == 'plus-circle'"><i class="fa fa-plus-circle"></i></t>
                                    <t t-elif="act.icon == 'edit-3'"><i class="fa fa-pencil"></i></t>
                                    <t t-elif="act.icon == 'user-plus'"><i class="fa fa-user-plus"></i></t>
                                    <t t-else=""><i class="fa fa-bell"></i></t>
                                </div>
                                <div class="dash-activity-time"><t t-esc="act.time"/></div>
                                <div class="dash-activity-text">
                                    <span class="dash-activity-user"><t t-esc="act.user"/></span>
                                    <span> <t t-esc="act.action"/></span>
                                </div>
                            </div>
                        </t>
                        <div class="text-center py-4 text-muted" t-if="!state.data.recent_activity or !state.data.recent_activity.length">
                            <i class="fa fa-history mb-2" style="font-size: 24px; opacity: 0.4;"></i>
                            <div style="font-size: 13px;">No recent activity</div>
                        </div>
                    </div>
                    <div class="dash-activity-footer">
                        <a class="dash-activity-view-all" t-on-click="() => this.onOpenAllActivity()">
                            <span>View All Activity</span>
                            <i class="fa fa-arrow-right ms-1"></i>
                        </a>
                    </div>
                </div>
            </div>
        </div>
    <!-- ══ Unified Single-Page Task Details & Analytics Popup Modal ══════════════ -->
    <t t-if="state.showTaskDetailModal">
        <div class="pd-modal-overlay" t-on-click="closeTaskDetailModal">
            <div class="pd-task-popup-modal" t-on-click.stop="">
                <!-- Header -->
                <div class="pd-task-popup-hdr">
                    <div class="pd-task-popup-hdr-top">
                        <span class="pd-task-popup-title pd-task-popup-title-link" t-att-title="'Click to open task in Odoo' + (state.taskDetailData ? ': ' + state.taskDetailData.name : '')" t-on-click="() => this.openTask(state.taskDetailData ? state.taskDetailData.id : null)">
                            Task Details - <t t-esc="state.taskDetailData ? (state.taskDetailData.name || state.taskDetailData.task) : 'Loading...'"/>
                        </span>
                        <div style="display: flex; align-items: center; gap: 10px;">
                            <button type="button" class="pd-btn-open-task" t-if="state.taskDetailData &amp;&amp; state.taskDetailData.id" t-on-click="() => this.openTask(state.taskDetailData.id)" title="Open full task in Odoo">
                                <i class="fa fa-external-link"/> <span>Open Task</span> <span>↗</span>
                            </button>
                            <span class="pd-modal-close" style="font-size: 16px; cursor: pointer; color: #94a3b8;" t-on-click="closeTaskDetailModal">✕</span>
                        </div>
                    </div>
                </div>

                <!-- Scrollable Body (All information in one seamless page) -->
                <div class="pd-task-popup-body">
                    <t t-if="state.taskDetailLoading">
                        <div style="display: flex; align-items: center; justify-content: center; padding: 60px 0; color: #64748b; gap: 10px;">
                            <i class="fa fa-circle-o-notch fa-spin fa-2x"/>
                            <span style="font-size: 14px; font-weight: 500;">Loading task details...</span>
                        </div>
                    </t>
                    <t t-elif="state.taskDetailData">
                        
                        <!-- ── 1. UNEDITABLE FORM CONTROLS GRID ── -->
                        <div class="pd-task-form-grid">
                            <div class="pd-form-field">
                                <label class="pd-form-lbl">Project</label>
                                <div class="pd-form-input-readonly">
                                    <span t-esc="state.taskDetailData.project || 'No Project'"/>
                                    <span class="pd-chevron">▼</span>
                                </div>
                            </div>
                            <div class="pd-form-field">
                                <label class="pd-form-lbl">Department</label>
                                <div class="pd-form-input-readonly">
                                    <span t-esc="state.taskDetailData.department || 'General'"/>
                                    <span class="pd-chevron">▼</span>
                                </div>
                            </div>
                            <div class="pd-form-field">
                                <label class="pd-form-lbl">Status</label>
                                <div class="pd-form-input-readonly">
                                    <span t-esc="state.taskDetailData.status_label || 'In Progress'"/>
                                    <span class="pd-chevron">▼</span>
                                </div>
                            </div>
                            <div class="pd-form-field">
                                <label class="pd-form-lbl">Due Date</label>
                                <div class="pd-form-input-readonly">
                                    <span t-esc="state.taskDetailData.due_date_dmy || state.taskDetailData.due_date || ''"/>
                                    <i class="fa fa-calendar" style="color: #64748b; font-size: 13px;"/>
                                </div>
                            </div>
                            <div class="pd-form-field">
                                <label class="pd-form-lbl">Stage &amp; Priority</label>
                                <div class="pd-form-input-readonly">
                                    <span>
                                        <t t-esc="state.taskDetailData.stage || 'New'"/>
                                        <t t-if="state.taskDetailData.priority_label">
                                            • <t t-esc="state.taskDetailData.priority_label"/>
                                        </t>
                                    </span>
                                    <i class="fa fa-tag" style="color: #94a3b8; font-size: 12px;"/>
                                </div>
                            </div>
                            <div class="pd-form-field">
                                <label class="pd-form-lbl">Created By</label>
                                <div class="pd-form-input-readonly">
                                    <div style="display: flex; align-items: center; gap: 7px;">
                                        <img t-if="state.taskDetailData.created_by_avatar" t-att-src="state.taskDetailData.created_by_avatar" style="width: 20px; height: 20px; border-radius: 50%; object-fit: cover;"/>
                                        <span t-else="" class="pd-assignee-avatar" style="width: 20px; height: 20px; font-size: 9px;"><t t-esc="state.taskDetailData.created_by_initials || 'U'"/></span>
                                        <span t-esc="state.taskDetailData.created_by || 'System'"/>
                                    </div>
                                    <i class="fa fa-user-o" style="color: #94a3b8; font-size: 12px;"/>
                                </div>
                            </div>
                        </div>

                        <!-- ── 2. ASSIGNED EMPLOYEES ── -->
                        <div class="pd-form-field">
                            <label class="pd-form-lbl">Assigned Employee(s)</label>
                            <div class="pd-assignee-chips-wrap">
                                <t t-if="state.taskDetailData.assignees &amp;&amp; state.taskDetailData.assignees.length &gt; 0">
                                    <t t-foreach="state.taskDetailData.assignees" t-as="asgn" t-key="asgn.id">
                                        <div class="pd-assignee-chip" style="cursor: pointer;" t-on-click="() => this.openPersonCard(asgn.id)">
                                            <img t-if="asgn.avatar" t-att-src="asgn.avatar" style="width: 20px; height: 20px; border-radius: 50%; object-fit: cover;"/>
                                            <span t-else="" class="pd-assignee-avatar"><t t-esc="asgn.initials"/></span>
                                            <span t-esc="asgn.name"/>
                                        </div>
                                    </t>
                                </t>
                                <t t-elif="state.taskDetailData.employee &amp;&amp; state.taskDetailData.employee !== 'Unassigned'">
                                    <t t-foreach="state.taskDetailData.employee.split(',')" t-as="empName" t-key="empName">
                                        <div class="pd-assignee-chip">
                                            <span class="pd-assignee-avatar"><t t-esc="this.getInitials(empName)"/></span>
                                            <span t-esc="empName.trim()"/>
                                        </div>
                                    </t>
                                </t>
                                <t t-else="">
                                    <span style="font-size: 13px; color: #64748b; font-style: italic;">Unassigned</span>
                                </t>
                            </div>
                        </div>

                        <!-- ── 3. DESCRIPTION & NOTES ── -->
                        <div class="pd-form-field">
                            <label class="pd-form-lbl"><i class="fa fa-align-left" style="margin-right: 4px;"/> Description / Notes</label>
                            <div class="pd-task-desc-card">
                                <t t-if="state.taskDetailData.description">
                                    <div t-out="renderMarkup(state.taskDetailData.description)"/>
                                </t>
                                <t t-else="">
                                    <span style="color: #94a3b8; font-style: italic;">No description notes entered for this task.</span>
                                </t>
                            </div>
                        </div>

                        <!-- ── 4. PROJECT STAGE PROGRESSION ANALYTICS (PIE CHART) ── -->
                        <div class="pd-task-analytics-card">
                            <div class="pd-task-analytics-title">PROJECT STAGE PROGRESSION ANALYTICS</div>
                            <div class="pd-analytics-body">
                                <!-- Pie Chart: Current Stage (Green) vs Remaining Stages Pending (Amber) -->
                                <div class="pd-pie-chart-wrap">
                                    <div t-att-style="'width: 100px; height: 100px; border-radius: 50%; background: conic-gradient(#10b981 0% ' + (state.taskDetailData.analytics?.stage_progress_pct || 0) + '%, #f59e0b ' + (state.taskDetailData.analytics?.stage_progress_pct || 0) + '% 100%); box-shadow: 0 2px 8px rgba(0,0,0,0.08);'"/>
                                </div>

                                <!-- Legend -->
                                <div class="pd-analytics-legend">
                                    <div class="pd-legend-item">
                                        <div class="pd-legend-label">
                                            <span class="pd-legend-box pd-legend-box-green"/>
                                            <span>Current Stage (<t t-esc="state.taskDetailData.stage || 'In Progress'"/>)</span>
                                        </div>
                                        <span class="pd-legend-pct" style="color: #059669;">
                                            <t t-esc="(state.taskDetailData.analytics?.stage_progress_pct || 0).toFixed(2)"/>%
                                            <span style="font-size: 11px; font-weight: normal; color: #64748b; margin-left: 3px;">
                                                (Stage <t t-esc="state.taskDetailData.analytics?.current_stage_idx || 0"/> of <t t-esc="state.taskDetailData.analytics?.total_stages || 0"/>)
                                            </span>
                                        </span>
                                    </div>
                                    <div class="pd-legend-item">
                                        <div class="pd-legend-label">
                                            <span class="pd-legend-box pd-legend-box-amber"/>
                                            <span>Remaining Stages Pending</span>
                                        </div>
                                        <span class="pd-legend-pct" style="color: #d97706;">
                                            <t t-esc="(state.taskDetailData.analytics?.remaining_stage_pct || 0).toFixed(2)"/>%
                                            <span style="font-size: 11px; font-weight: normal; color: #64748b; margin-left: 3px;">
                                                (<t t-esc="state.taskDetailData.analytics?.remaining_stages_count || 0"/> left)
                                            </span>
                                        </span>
                                    </div>
                                    <div class="pd-analytics-subtext">
                                        Calculated from project stage sequence (<t t-esc="state.taskDetailData.analytics?.current_stage_idx || 0"/> of <t t-esc="state.taskDetailData.analytics?.total_stages || 0"/> workflow stages completed).
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- ── 5. DISCUSSION NOTES TEXTAREA ── -->
                        <div class="pd-form-field">
                            <label class="pd-form-lbl"><i class="fa fa-commenting-o" style="margin-right: 4px;"/> Discussion Notes</label>
                            <textarea class="pd-discussion-textarea" placeholder="Write discussion updates or analytics details..." t-model="state.discussionNoteInput"/>
                        </div>

                        <!-- ── 6. TASK HISTORY & STAGE MOVES ── -->
                        <div class="pd-task-history-card">
                            <div class="pd-task-history-title">TASK HISTORY &amp; STAGE MOVES</div>
                            <div class="pd-history-list" style="max-height: 260px;">
                                <t t-if="getTaskHistoryEntries().length === 0">
                                    <div style="color: #94a3b8; font-size: 13px; font-style: italic; padding: 18px 0; text-align: center;">
                                        No history or stage moves recorded yet.
                                    </div>
                                </t>
                                <t t-else="">
                                    <t t-foreach="getTaskHistoryEntries()" t-as="hist" t-key="hist.id">
                                        <div class="pd-history-item">
                                            <div class="pd-history-avatar-circle">
                                                <t t-esc="hist.initials"/>
                                            </div>
                                            <div class="pd-history-content">
                                                <div class="pd-history-author-row">
                                                    <span t-esc="hist.author"/>
                                                    <span class="pd-history-time" t-esc="hist.time_str"/>
                                                </div>
                                                <t t-if="hist.tracking_values &amp;&amp; hist.tracking_values.length > 0">
                                                    <t t-foreach="hist.tracking_values" t-as="trk" t-key="trk_index">
                                                        <div class="pd-history-change-line">
                                                            <span>• <t t-esc="trk.field"/>:</span>
                                                            <span class="pd-history-old-val" t-esc="trk.old_value"/>
                                                            <span class="pd-history-arrow">➔</span>
                                                            <span class="pd-history-new-val" t-esc="trk.new_value"/>
                                                        </div>
                                                    </t>
                                                </t>
                                                <t t-if="hist.body">
                                                    <div style="font-size: 12.5px; color: #334155; margin-top: 2px;" t-out="renderMarkup(hist.body)"/>
                                                </t>
                                            </div>
                                        </div>
                                    </t>
                                </t>
                            </div>
                        </div>

                    </t>
                </div>

                <!-- Footer -->
                <div class="pd-task-popup-ftr">
                    <button type="button" class="pd-btn-save-blue" t-att-disabled="state.isSavingDiscussion" t-on-click="saveDiscussion">
                        <t t-if="state.isSavingDiscussion">Saving...</t>
                        <t t-else="">Save Discussion</t>
                    </button>
                    <button type="button" class="pd-btn-outline" t-on-click="closeTaskDetailModal">Close</button>
                </div>
            </div>
        </div>
    </t>

    </t>
</templates>
