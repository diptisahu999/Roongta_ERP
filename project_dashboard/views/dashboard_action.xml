<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ── Client Action ─────────────────────────────────────────────────────── -->
    <record id="action_project_dashboard" model="ir.actions.client">
        <field name="name">Project Dashboard</field>
        <field name="tag">project_dashboard_action</field>
    </record>

    <!-- ── Menu Item — first entry under Project app (sequence=0) ────────────── -->
    <menuitem
        id="menu_project_dashboard"
        name="Dashboard"
        parent="project.menu_main_pm"
        action="action_project_dashboard"
        sequence="0"
        groups="project.group_project_user"
    />
    <!-- ── Make List View the default for Project Tasks ──────────────────────── -->
    <record id="project.act_project_project_2_project_task_all" model="ir.actions.act_window">
        <field name="view_mode">list,kanban,form,calendar,pivot,graph,activity</field>
    </record>
    
    <!-- We also MUST override the sequence of the view bindings, because Odoo prioritizes sequence over view_mode string -->
    <record id="project.project_task_tree_action_view" model="ir.actions.act_window.view">
        <field name="sequence" eval="5"/>
    </record>

</odoo>
