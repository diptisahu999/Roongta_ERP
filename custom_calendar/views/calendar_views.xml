<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Client action for Custom Meeting Calendar -->
        <record id="action_custom_calendar" model="ir.actions.client">
            <field name="name">Meeting Calendar</field>
            <field name="tag">custom_calendar_action</field>
            <field name="target">current</field>
        </record>
    </data>
</odoo>
